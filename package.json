{
  "name": "page-builder",
  "private": true,
  "scripts": {
    "build": "turbo build",
    "dev": "turbo dev",
    "lint": "turbo lint",
    "format": "prettier --write \"**/*.{ts,tsx,md}\"",
    "publishing:patch": "pnpm run publishing:check && pnpm run publishing:prepareFiles && cd ./dist && npm version patch && pnpm run publishing:push && pnpm run publishing:cleanup",
    "publishing:minor": "pnpm run publishing:check && pnpm run publishing:prepareFiles && cd ./dist && npm version minor && pnpm run publishing:push && pnpm run publishing:cleanup",
    "publishing:major": "pnpm run publishing:check && pnpm run publishing:prepareFiles && cd ./dist && npm version major && pnpm run publishing:push && pnpm run publishing:cleanup",
    "publishing:check": "git diff --exit-code --name-only && git checkout master && git pull",
    "publishing:prepareFiles": "cp -r ./packages/next-page-builder ./dist",
    "publishing:push": "npm publish",
    "publishing:cleanup": "cd ../ && rm -rf ./dist"
  },
  "devDependencies": {
    "@repo/typescript-config": "workspace:*",
    "prettier": "^3.1.0",
    "turbo": "latest"
  },
  "packageManager": "pnpm@8.9.0",
  "engines": {
    "node": ">=18"
  }
}
